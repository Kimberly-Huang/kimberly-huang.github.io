---
import type { Project } from "../data/projects";
import ProjectPreview from "./ProjectPreview.astro";

interface Props {
  project: Project;
  compact?: boolean;
}

const { project, compact = false } = Astro.props;
---

<article class:list={["project-card", compact && "project-card-compact"]}>
  <ProjectPreview kind={project.preview} />
  <div class="project-copy">
    <div class="project-kicker">
      <span class="project-number">{project.number}</span>
      <span>{project.eyebrow}</span>
      {project.award && <strong>{project.award}</strong>}
    </div>
    <h3>{project.title}</h3>
    <p>{project.summary}</p>
    {!compact && (
      <dl class="project-evidence">
        <div><dt>My role</dt><dd>{project.contribution}</dd></div>
        <div><dt>Outcome</dt><dd>{project.outcome}</dd></div>
      </dl>
    )}
    <div class="project-footer">
      <ul aria-label={`${project.title} technologies`}>
        {project.tags.map((tag) => <li>{tag}</li>)}
      </ul>
      <div class="project-links">
        {!compact && <a class="project-action project-action-primary" href={`/work/${project.slug}`}>Read case study <span aria-hidden="true">→</span></a>}
        {project.demo && <a class="project-action" href={project.demo} target="_blank" rel="noreferrer">Live product <span aria-hidden="true">↗</span></a>}
        {project.repo && <a class="project-action" href={project.repo} target="_blank" rel="noreferrer">Source code <span aria-hidden="true">↗</span></a>}
      </div>
    </div>
  </div>
</article>
